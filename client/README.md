# TrendScout Client

Vite + React + TypeScript frontend для анализа трендов TikTok.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
VITE_API_URL=http://localhost:8000/api
```

**Для production:**
```env
VITE_API_URL=https://your-backend-url.com/api
```

### 3. Запуск dev сервера

```bash
npm run dev
```

Приложение запустится на: **http://localhost:5173**

### 4. Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 🛠 Технологии

- **Vite 7** - Build tool
- **React 19** - UI library
- **TypeScript** - Type safety
- **Tailwind CSS** - Styling
- **shadcn/ui** - UI components
- **React Router** - Routing

## 📁 Структура проекта

```
src/
├── components/     # UI компоненты
├── pages/          # Страницы приложения
├── hooks/          # React hooks
├── services/       # API клиент
├── types/          # TypeScript типы
└── lib/            # Утилиты
```

## 🌐 Деплой

### Vercel

1. Подключите репозиторий к Vercel
2. Установите переменную окружения `VITE_API_URL`
3. Deploy

### Netlify

1. Подключите репозиторий к Netlify
2. Build command: `npm run build`
3. Publish directory: `dist`
4. Добавьте переменную окружения `VITE_API_URL`

### Другие платформы

Загрузите содержимое папки `dist/` на любой статический хостинг.

## 🔧 Настройка

### Изменение API URL

Обновите `VITE_API_URL` в `.env` файле или в настройках вашего хостинга.

### Кастомизация

- Стили: `src/index.css` и `tailwind.config.js`
- Компоненты: `src/components/`
- Страницы: `src/pages/`

## 📝 Лицензия

Создано для образовательных и коммерческих целей.
