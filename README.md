# TrendScout MVP - Инструкция по запуску

## 📋 Что требуется для запуска

### 1. **API ключи (обязательно)**

Вам нужны два API ключа:

#### a) Apify API Token
- Зарегистрируйтесь на [https://console.apify.com](https://console.apify.com)
- Перейдите в раздел **Account → Integrations**
- Скопируйте ваш **API Token**

#### b) Google Gemini API Key
- Зарегистрируйтесь на [https://makersuite.google.com/app/apikey](https://makersuite.google.com/app/apikey)
- Создайте новый API ключ
- Скопируйте ключ

### 2. **Установка зависимостей**

Убедитесь, что у вас установлен Python 3.10 или выше, затем выполните:

```bash
pip install -r requirements.txt
```

### 3. **Настройка переменных окружения**

Создайте файл `.env` в корневой папке проекта со следующим содержимым:

```env
APIFY_API_TOKEN=ваш_apify_токен_здесь
GEMINI_API_KEY=ваш_gemini_ключ_здесь
```

**Важно:** Замените `ваш_apify_токен_здесь` и `ваш_gemini_ключ_здесь` на реальные значения.

### 4. **Настройка бизнес-ключевых слов (опционально)**

По умолчанию система ищет контент с ключевыми словами:
- `coffee`, `latte`, `espresso`
- `бариста`, `кофе`, `алматы`

Если вам нужны другие ключевые слова, отредактируйте файл `src/filter.py`, изменив список `business_keywords` в конструкторе класса `ViralContentFilter`.

## 🚀 Запуск приложения

### Вариант 1: Веб-интерфейс (Streamlit Dashboard)

```bash
streamlit run dashboard.py
```

Откроется браузер с интерфейсом, где вы сможете:
- Вводить ключевые слова для поиска
- Запускать анализ трендов
- Просматривать результаты
- Управлять базой данных

### Вариант 2: Командная строка (Headless)

```bash
python main.py
```

Это запустит анализ с примерными ключевыми словами (`кофе`, `бариста`).

Чтобы изменить ключевые слова, отредактируйте последние строки в `main.py`:

```python
if __name__ == "__main__":
    sample_keywords = ["ваши", "ключевые", "слова"]
    run_analysis(sample_keywords)
```

## 📊 Структура проекта

```
trendscout/
├── .env                    # Ваши API ключи (создайте сами)
├── database.py             # Модель базы данных
├── main.py                 # Оркестратор (headless режим)
├── dashboard.py            # Streamlit интерфейс
├── requirements.txt        # Зависимости Python
├── trendscout.db          # База данных SQLite (создаётся автоматически)
└── src/
    ├── __init__.py
    ├── collector.py        # Сбор данных через Apify
    ├── filter.py           # Фильтрация по ключевым словам
    ├── finder.py           # Группировка похожих трендов
    └── scorer.py           # Расчёт вирусного потенциала
```

## ⚙️ Дополнительные настройки

### Минимальное количество просмотров

По умолчанию фильтруются видео с менее чем 1000 просмотров. Чтобы изменить это значение, отредактируйте `src/filter.py`:

```python
content_filter = ViralContentFilter(min_views=5000)  # например, 5000
```

### Лимит результатов на ключевое слово

В `main.py` можно изменить количество результатов на каждое ключевое слово:

```python
raw_items = collector.collect(keywords_list, limit_per_keyword=50)  # вместо 25
```

## 🔍 Как это работает

1. **Сбор данных**: Система использует Apify для поиска видео TikTok по вашим ключевым словам
2. **Фильтрация**: Отсеиваются видео без бизнес-ключевых слов и с низкой активностью
3. **Группировка**: Похожие видео объединяются в тренды (похожесть текста ≥60%)
4. **Оценка**: Каждому тренду присваивается UTS (Universal Trend Score) на основе:
   - Engagement Rate (лайки, комментарии, репосты)
   - Velocity Bonus (бонус за высокие просмотры)
   - Group Bonus (бонус за размер группы похожих видео)
5. **AI-анализ**: Gemini Flash генерирует краткое резюме на русском языке
6. **Сохранение**: Результаты сохраняются в SQLite базу данных

## ❓ Решение проблем

### Ошибка "APIFY_API_TOKEN is required"
- Убедитесь, что файл `.env` создан и содержит правильный токен
- Проверьте, что токен не содержит лишних пробелов или кавычек

### Ошибка при вызове Gemini API
- Проверьте правильность `GEMINI_API_KEY` в `.env`
- Убедитесь, что у вас есть доступ к API Gemini

### База данных не создаётся
- Убедитесь, что у вас есть права на запись в папку проекта
- Проверьте, что SQLAlchemy установлен корректно

## 📝 Примечания

- База данных SQLite создаётся автоматически при первом запуске
- Дубликаты видео (по URL) автоматически пропускаются
- Все временные метки сохраняются в UTC
